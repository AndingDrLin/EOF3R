# EOF3R 完整依赖清单
# 锁定版本来自 eof3r env (Python 3.10, torch 2.5.1+cu121)
# 安装: pip install -r requirements.txt

# ---- 深度学习核心 ----
torch>=2.5.0
torchvision>=0.20.0
pytorch-lightning>=2.0.0  # MVSplat / training loop
lightning-utilities

# ---- Feedforward 3DGS 推理 ----
hydra-core>=1.3.0           # MVSplat config (Hydra compose)
diff_gaussian_rasterization # 3DGS rasterizer (custom build, see scripts/setup_mvsplat.sh)
einops>=0.8.0               # tensor manipulation
timm>=1.0.0                 # ViT backbone (VGGT)
huggingface-hub>=0.20.0     # HF model download (SAM2, VGGT)
omegaconf>=2.3.0

# ---- 3D geometry / point cloud ----
plyfile
trimesh
open3d                      # point cloud I/O & visualization (install may be slow)
numpy<2.0                   # pin <2.0 for compatibility with older MVSplat code
scipy>=1.10.0

# ---- 2D image & vision ----
opencv-python>=4.8.0
pillow>=10.0.0
matplotlib>=3.7.0           # visualization in test scripts
scikit-image>=0.21.0        # SAM2 image ops
lpips>=0.1.4                # auxiliary perceptual metric

# ---- configuration & CLI ----
pyyaml>=6.0

# ---- logging & experiment tracking ----
wandb>=0.15.0               # experiment tracking (primary)

# ---- utilities ----
tqdm>=4.65.0
rich>=13.0.0
python-dotenv               # .env loading
